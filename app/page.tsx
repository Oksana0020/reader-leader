/* Reference-led rule: replicate the supplied story-library hierarchy before adding any secondary product chrome. */
import { StoryLibrary } from "@/components/story-library";

export default function StorySelectionPage() { return <StoryLibrary />; }
